package km;

import java.io.File;
import java.io.FileOutputStream;
import java.io.IOException;
import java.util.List;

import org.dom4j.Document;
import org.dom4j.DocumentException;
import org.dom4j.Element;
import org.dom4j.Node;
import org.dom4j.io.OutputFormat;
import org.dom4j.io.SAXReader;
import org.dom4j.io.XMLWriter;

public class Main {
    public static void main(String[] args) throws DocumentException, IOException {

    	 // Query.query_only(1,"id");  //查询第一个good节点的子节点id的内容
    	 // Query.query_all("name");  //查询所有good节点中的子节点name的内容

    	 // Increase.increase_childNode(1,"num","24");  //向第一个good节点中添加一个名为num的子节点，其值为24

    	 // Delete.delete(1, "num");  //删除第一个good节点的子节点num

    	 // Alter.alter(1, "num", "44");
    	
    	
    }
}