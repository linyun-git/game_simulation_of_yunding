import { StatusEqualsWaitingPipe } from './status-equals-waiting.pipe';

describe('StatusEqualsWaitingPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusEqualsWaitingPipe();
    expect(pipe).toBeTruthy();
  });
});
